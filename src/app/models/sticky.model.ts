export class Sticky {
    constructor(
        public title: string,
        public message?: string,
        public from?: string,
        public redeemed?: boolean,
        public reserved?: boolean
    ) {

    }
}
